<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neu.youthpathtalk.notification.infrastructure.mapper.NotificationMapper">
    <insert id="insert">
        INSERT INTO t_notification (
            receiver_id, sender_id, sender_name, sender_avatar,
            type, target_type, post_id, root_id, comment_id,
            target_title,target_content, content, create_time
        ) VALUES (
                     #{receiverId}, #{senderId}, #{senderName}, #{senderAvatar},
                     #{type}, #{targetType}, #{postId}, #{rootId}, #{commentId},
                     #{targetTitle},#{targetContent}, #{content}, #{createTime}
                 )
    </insert>

    <select id="selectByReceiverIdAndTypes"
            resultType="com.neu.youthpathtalk.notification.domain.vo.resp.NotificationRespVO">

        SELECT
        id,
        sender_id,
        sender_name,
        sender_avatar,
        type,
        target_type,
        post_id,
        root_id,
        comment_id,
        target_title,
        target_content,
        content,
        is_read,
        create_time
        FROM t_notification
        WHERE receiver_id = #{receiverId}

        <if test="types != null and types.size() > 0">
            AND type IN
            <foreach collection="types"
                     item="type"
                     open="("
                     separator=","
                     close=")">
                #{type}
            </foreach>
        </if>

        <if test="cursor != null">
            AND id &lt; #{cursor}
        </if>

        ORDER BY id DESC
        LIMIT #{limit}

    </select>

    <update id="updateReadByReceiverIdAndTypes">
        UPDATE t_notification
        SET is_read = 1
        WHERE receiver_id = #{userId}
        AND type IN
        <foreach collection='types' item='type' open='(' separator=',' close=')'>
            #{type}
        </foreach>
        AND is_read = 0
    </update>
</mapper>

